apiVersion: apps/v1
kind: Deployment
metadata:
  namespace: {{ .Values.namespace }}
  name: {{ include "name" . }}-postgrest
  labels:
    app: {{ include "name" . }}
    component: postgrest
spec:
  replicas: 1
  selector:
    matchLabels:
      app: {{ include "name" . }}
      component: postgrest
  template:
    metadata:
      labels:
        app: {{ include "name" . }}
        component: postgrest
    spec:
      initContainers:
        - name: wait-for-postgres
          image: postgres:16
          command:
            - sh
            - -c
            - |
              until pg_isready -h {{ include "name" . }}-postgres -p {{ .Values.postgres.port }}; do
                echo 'Waiting for PostgreSQL...'
                sleep 2
              done
      containers:
        - name: postgrest
          image: {{ .Values.postgrest.image }}
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 3000
              name: http
          envFrom:
            - secretRef:
                name: {{ include "name" . }}-postgrest
          env:
            - name: PGRST_SERVER_PORT
              value: "3000"
          livenessProbe:
            tcpSocket:
              port: 3000
            initialDelaySeconds: 60
            periodSeconds: 10
          readinessProbe:
            tcpSocket:
              port: 3000
            initialDelaySeconds: 15
            periodSeconds: 5
          {{- if .Values.postgrest.resources }}
          resources:
            {{- toYaml .Values.postgrest.resources | nindent 12 }}
          {{- end }}
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop:
                - ALL
      securityContext:
        runAsNonRoot: true
        runAsUser: 65534
