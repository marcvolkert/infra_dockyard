apiVersion: v1
kind: Service
metadata:
  namespace: {{ .Values.namespace }}
  name: {{ include "name" . }}-postgrest
  labels:
    app: {{ include "name" . }}
    component: postgrest
spec:
  type: ClusterIP
  selector:
    app: {{ include "name" . }}
    component: postgrest
  ports:
    - port: {{ .Values.postgrest.port }}
      targetPort: {{ .Values.postgrest.port }}
      name: http
